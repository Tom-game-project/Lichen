
pub fn gcd(a:i32,b:i32):i32{
    if (b == 0){
        return a;
    }else{
        return gcd(b,a%b);
    };
}

pub fn gcd2(a:i32,b:i32):i32{
    return if (0 == b)
    {
        a;
    }else{
        gcd(b,a%b);
    };
}

